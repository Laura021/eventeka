---
applications:
  .:
    name: Eventos
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 512M
    instances: 1
    services:
      eventos_development:
        type: mongodb
